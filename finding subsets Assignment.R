library(ISLR)
default_data = ISLR::Default
default_data
for(j in 1:10000){
      if(default_data$default[j] = "Yes"){
        default_data$default[j] == TRUE
       }
  else{
        default_data$default[j] == FALSE
            }
    }
for(i in 1:10){
  default_data$default[j] = "Yes"
}
default_data$default[1:10000] = "Yes"

